[project]
name = "expression-reexpressor"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "sympy",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
